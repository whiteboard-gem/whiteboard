#!/usr/bin/env ruby

require 'whiteboard'
require 'whiteboard/cli'
include Whiteboard::CLI

case ARGV[0]
when "new"
  system "touch Whiteboard"
  File.open('Whiteboard', 'w') { |file| file.write(file_string) }
when "run"
  puts "Generating your app based on your Whiteboard file"
end
